/**
 * Some constants used by the application
 */

const API = {
    COLABEL_PREDICTION: 'https://api.colabel.com/v1/models/195/prediction',
};
const ROUTES = {
    CLASSIFIED_IMAGES: '/classified-images',
    UPLOAD: '/upload',
    DASHBAORD: '/dashboard',
};
export const URLs = { API, ROUTES };
